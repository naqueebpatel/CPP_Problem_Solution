#include<iostream>
using namespace std;
class factor
{
	int num,i;
	public:
		int fac(int num)
		{
    int i;
	for(i=1;i<num;i++)
	{
	if(num%i==0)
	cout<<i<<endl;
	}
}

};
int main()
{
	 int num;
   cout<<"enter the number to find factors"<<endl;
	cin>>num;
	factor f;
	f.fac(num);
	return 0;
}
